interface:
  display_name: "Gstack"
  short_description: "Repo-specific patterns and workflows for Gstack"
  default_prompt: "Use the Gstack repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true